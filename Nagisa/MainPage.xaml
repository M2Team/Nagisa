<Page
    x:Class="Nagisa.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nagisa"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="NagisaTitleBar" Height="32" Background="LightGray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="toggleButton" 
                              IsChecked="{Binding ElementName=NagisaSplitView, Path=IsPaneOpen, Mode=TwoWay}">
                <ToggleButton.Content>
                    <FontIcon FontFamily="Segoe MDL2 Assets" Foreground="White" Glyph="&#xE700;"/>
                </ToggleButton.Content>
            </ToggleButton>
            <Grid x:Name="NagisaTitleBarBackground" Background="Transparent" Grid.Column="1">
                <TextBlock Margin="8" FontSize="12" Text="Nagisa" />
            </Grid>
            
        </Grid>

        <SplitView x:Name="NagisaSplitView" Grid.Row="1">
            <SplitView.Pane>
                <StackPanel VerticalAlignment="Stretch" Margin="8">
                    <TextBlock Text="About"/>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Nagisa [Version 0.1.1702.17]"
                                   Margin="8,8,0,0"
                                   TextWrapping="Wrap"/>
                        <TextBlock Text="&#xA9; 2017 M2-Team"
                                   Margin="8,8,0,0"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
            </SplitView.Pane>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <AutoSuggestBox Grid.Row="0" Height="32" PlaceholderText="Search Downloads" QueryIcon="Find" />

                <ListView x:Name="listView" Grid.Row="1">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>


                    <ListViewItem>


                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock VerticalAlignment="Center" Text="Test Item X"/>
                            <ProgressBar VerticalAlignment="Bottom" Value="50"  Grid.Row="1" Grid.ColumnSpan="4"/>
                            <AppBarButton Grid.Column="1" Icon="Pause" Label="Pause"/>
                            <AppBarButton Grid.Column="2" Icon="Delete" Label="Delete"/>
                            <AppBarButton Grid.Column="3" Icon="OpenLocal" Label="Open Folder"/>
                        </Grid>
                    </ListViewItem>

                    <ListViewItem Content="Test Item1"/>
                    <ListViewItem Content="Test Item2"/>
                    
                    
                </ListView>
                <CommandBar Grid.Row="2">
                    
                    <AppBarToggleButton Icon="Add" Label="Add Task" />
                    <AppBarToggleButton Icon="Clear" Label="Clear List"/>
                    <CommandBar.Content>
                        <TextBlock Text="Total Bandwidth: 1024 KiB/s" Margin="12,14"/>
                    </CommandBar.Content>
                </CommandBar>
            </Grid>
        </SplitView>
    </Grid>
</Page>
